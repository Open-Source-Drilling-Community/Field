apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "osdcdrillingfieldservice.fullname" . }}
  labels:
    {{- include "osdcdrillingfieldservice.labels" . | nindent 4 }}
spec:
  strategy:
    type: {{ .Values.strategy.type }}
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "osdcdrillingfieldservice.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "osdcdrillingfieldservice.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "osdcdrillingfieldservice.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          {{- if .Values.probes.enabled}}
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          {{- end}}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
          - mountPath: /home/
            name: fielddb
          {{- if .Values.projectionDefinitionIdMappings }}
          - mountPath: /config/Field.ProjectionMappings.json
            name: projection-mappings
            subPath: Field.ProjectionMappings.json
            readOnly: true
          env:
          - name: FIELD_PROJECTION_MAPPINGS_CONFIG
            value: /config/Field.ProjectionMappings.json
          {{- end }}
      volumes:
      - name: fielddb
        {{- if .Values.persistence.enabled }}
        persistentVolumeClaim:
          claimName: {{ .Values.persistence.existingClaim | default .Values.persistence.claimName }}
        {{- else }}
        emptyDir: {}
        {{- end }}
      {{- if .Values.projectionDefinitionIdMappings }}
      - name: projection-mappings
        configMap:
          name: {{ include "osdcdrillingfieldservice.fullname" . }}-projection-mappings
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
